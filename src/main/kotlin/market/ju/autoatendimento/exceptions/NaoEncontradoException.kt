package market.ju.autoatendimento.exceptions

class NaoEncontradoException(message: String): RuntimeException(message) {
    //Exceção criada para retornar uma mensagem quando o objeto não for encontrado
}